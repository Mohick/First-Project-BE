<div class="  fixed flex justify-center items-center top-0 left-0 right-0 bottom-0 bg-white z-20">
    <div>
        <div id="loading">
            <span class="inline-block capitalize l">
                Error 404 click back home : <a class="text-blue-800 underline" href="/">back Home</a>
            </span>
        </div>
    </div>
</div>
{{!--

<style>
    #loading {
        filter: drop-shadow(0 0 1px black);

    }

    #loading>.l {
        animation: animationDropTopDown 1s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.8s, 3.8s;

    }

    #loading>.o {
        animation: animationDropTopDown 1.1s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.7s, 3.7s;


    }

    #loading>.a {
        animation: animationDropTopDown 1.2s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.6s, 3.6s;

    }

    #loading>.d {
        animation: animationDropTopDown 1.3s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.5s, 3.5s;

    }

    #loading>.i {
        animation: animationDropTopDown 1.4s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.4s, 3.4s;

    }

    #loading>.n {
        animation: animationDropTopDown 1.5s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.3s, 3.3s;

    }

    #loading>.g {
        animation: animationDropTopDown 1.6s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.1s, 3.2s;

    }

    #loading>.dot {
        animation: animationFromRightTo 1.7s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.0s, 3.1s;
    }

    #loading>.dot-1 {
        animation: animationFromRightTo 1.8s linear, animationPushed 1s, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 1.9s, 3s;
    }

    #loading>.dot-2 {
        animation: animationFromRightTo 1.9s linear, animationProgressLoading 1s infinite;
        filter: drop-shadow(0 3px 2px black);
        animation-delay: 0s, 2.9s;

    }

    @keyframes animationDropTopDown {
        0% {
            transform: translateY(-600px);
        }

        100% {
            transform: translateY(0);
        }
    }

    @keyframes animationFromRightTo {
        0% {
            transform: translateX(600px);
        }

        100% {
            transform: translateX(0);
        }
    }

    @keyframes animationPushed {
        0% {
            transform: skew(40deg, 0deg);

        }

        50% {
            transform: skew(-40deg, 0deg);
        }

        100% {
            transform: skew(0deg, 0deg);
        }
    }


    @keyframes animationProgressLoading {
        0% {
            transform: translateY(0);
        }

        50% {
            transform: translateY(15px);
        }

        100% {
            transform: translateY(0);
        }
    }
</style> --}}